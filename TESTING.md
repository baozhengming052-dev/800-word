# 验证记录

## 2026-09-25 每日学词乱序（源码计划版本 v2.4.9）

- 首页新开一组时，从所有未学词中随机抽取并排列，而不是取 PDF 顺序的前几项；打开学习页后顺序固定。
- 手动验收：开始一组、学习数词后点“稍后继续”，再次进入应恢复相同顺序和位置；旧版本未完成组升级后保留进度、仅剩余词打乱一次；完成该组后再开启新组应重新抽取。

## 2026-09-25 拍照插入与笔记阅读页修复（源码计划版本 v2.4.8）

- 已检查用户录屏：拍照后点“使用照片”，编辑页短暂显示“正在处理图片”，随后整页退出且词条中没有笔记。
- Windows 本机已通过 46 项 Python 回归、865 词/974 题与工程配置校验、32 个 Swift 文件语法解析，以及 `git diff --check`；这些检查不能替代 Xcode 编译和真机拍照验证。
- 需在 iPhone 上核对拍照、选图、取消、图片处理失败、保存、重新打开和阅读页返回；确认关闭的只有系统取图界面，草稿及图片仍在编辑页。
- 备份、导入和附近同步共同使用 100 MB 上限，单张图片上限 5 MB；两台设备都需升级以使用同步内容能力 6。

## 2026-09-25 图文笔记迭代（源码计划版本 v2.4.7）

- Windows：Python 回归与工程/权限配置校验；当前环境缺少 Xcode，不能在本机编译 iOS 页面或调用真机相机。
- macOS：`tools/verify_macos.sh` 新增 `RichNotesTests.swift`，覆盖旧文字笔记、图片备份引用、跨词条引用拒绝及双机图文合并；随后执行 Xcode archive。
- 真机：在 iPhone 与 iPad 分别测试相册选择、首次相机授权与拒绝后的提示、文字和图片交错、编辑删除、历史展开、备份导出/导入及双机冲突合并。相册使用系统选择器，只授权选中的照片。

## 2026-09-18 双校联合标识与开屏（源码计划版本 v2.4.4）

- 新增短时原生 SwiftUI 开屏：湖南工程学院和天津商业大学标识从两侧汇合，随后出现 App 名称并自动淡出；不使用视频、网络或第三方动画库。
- 首页顶部增加紧凑联合标识卡。开屏与首页均直接使用用户提供的 SVG，资源目录声明保留矢量；iPad 横屏使用更宽间距，深色模式下 Logo 置于白色品牌卡内保持原色可读。
- 开启系统“减弱动态效果”时不执行位移、弹簧和分段显现，只保留短暂停留与淡出。两个联合标识均合并无障碍朗读，装饰图不重复朗读。
- 未改动 Bundle Identifier、`Info.plist`、工程 target、学习数据、备份/同步协议、题库词库及 GitHub 工作流；Assets.xcassets 已由工程原有资源阶段统一收录，无需修改 `project.pbxproj`。
- 本地回归增加 `tests/test_collaboration_branding.py`，检查两个 SVG 资源、imageset 元数据、开屏自动关闭、减弱动态效果和首页入口；Python 全量回归 45 项、865词/974题工程校验、SVG XML 完整性与 `git diff --check` 已通过。项目自带的 tree-sitter 依赖缺失 `_binding`，本机因此未完成 Swift 语法解析；Windows 也无 Xcode，本版本仍须以 GitHub macOS 构建及 iPhone/iPad 真机显示为最终验收。

## 2026-09-18 词条近义词补充（源码计划版本 v2.4.3）

### 改动范围

- 新增“近义词 · 我的补充”：词条详情在“关联词与辨析”下方显示自己补充的近义词与区别说明，原资料内容始终按原文呈现，互不覆盖。原资料没有近义词的词也能直接添加。
- 存储沿用一条不可变事件：`kind = "synonym"`，值是整份列表的规范 JSON（键排序、去首尾空格、按词语去重）。仍然写在现有 v3 快照里，**没有新增数据文件、没有改快照版本、没有改学习事件格式**；最新一条生效，历史全部保留。
- 写入只增量追加一条事件，不重建词库、搜索索引和题目目录，也不重算完整学习历史。
- 同步：`SyncConflict.Kind` 增加 `.synonym`。两台分别改过同一个词时给出“保留本机 / 保留对方 / 两份近义词合并”，选择结果写成一条新事件，旧内容保留在修改历史里。内容能力从 3 升到 4，只升级一台时旧版会明确要求更新 App，而不是收到无法解析的记录。
- 未改动：Bundle Identifier、词库与题库内容、`Info.plist`、提醒与复习规则、`.github/workflows/build.yml`。没有新增源文件，因此 `project.pbxproj` 未动。

### 本地已执行

- Python 回归 42 项通过（新增 `tests/test_personal_synonyms.py` 6 项）。数据与工程检查通过：865 词、974 题、原 PDF 文本与哈希、资源布局、工程引用、Scheme、Info.plist 和图标。
- 31 个 Swift 文件 tree-sitter 语法解析通过，`git diff --check` 通过。Windows 没有 swiftc / Xcode，**语法解析不等于类型检查或 iOS 编译**。
- 搜索索引有意不纳入补充近义词：v2.3.1 已把搜索索引改为只在内容变化时重建，为保住连续学习不重建索引的性能边界，本次没有把事件数据混进索引。

### 需在 GitHub macOS 执行的原生测试

- `LearningEngineTests`：补充近义词按词条归约、最新列表生效且历史保留、输入顺序无关、非规范载荷被拒绝、无法解析的载荷不影响其他记录、增量应用与完整重算一致。
- `SyncMergeEngineTests`：两边不同补充产生 `.synonym` 冲突而不是按时钟覆盖、冲突界面显示可读文本、保留本机与两份合并的结果、清空一边仍是有效选择、单边补充不打扰用户、非规范载荷无法进入记录。
- `SyncExchangeTests`：内容能力 4，旧版（能力 3）在交换前被拒绝并给出更新提示。
- `tools/verify_macos.sh` 仍会编译运行全部原有测试。

### GitHub 构建与发布结果（2026-09-18）

- 提交 `b31500f`、Tag `v2.4.3` 推送后 Actions 构建通过并正式发布 **Release v2.4.3**（2026-09-18T08:16:54Z）。附件为 `政名政利公考800词-v2.4.3.ipa`（1.7 MB，SHA-256 `29edb36f3257ca005233daca203aa83f88f31b5e6f8bb7624bb8cbe516a29ff7`）、同名 `.sha256` 和 `release-metadata.json`。
- 包内版本 2.4.3，Bundle Identifier 仍是 `com.peanut13.words800`；发布说明由 README 的 `### v2.4.3` 段自动生成，内容与本节一致。
- 构建成功即代表这一步已在 GitHub macOS 上执行：Python 全量回归、`tools/verify_macos.sh` 的原生 Swift 测试（含上面列出的新增断言）、Xcode 归档、原生 Bundle 与 IPA 校验。Windows 本地无法执行的部分到此已覆盖。
- 以上证明的是源码可编译、原生测试通过、安装包已产出。**设备上的实际界面与双机同步仍未记录结果**，见下节。

### 真机待验收（尚未记录结果）

1. 两台先导出备份，再用 TrollStore 覆盖安装同一份 v2.4.3。只升级一台时，同步应提示“请更新两台设备的 App”，而不是报记录无效。
2. iPhone：打开已有辨析的词（如“一脉相承”），在“近义词 · 我的补充”添加一条并填写区别说明，保存后立即显示；再打开一个原资料没有辨析的词，确认可以添加且原资料区域不变。
3. 编辑、删除、同名词条覆盖、区别说明留空、只填近义词直接点“保存”，都应有明确结果或提示；把词条本身填成近义词应被拒绝。
4. 退出 App 重开、切换词条、iPad 横屏双栏与竖屏检查显示；编辑未保存时旋转，草稿保留，取消不写入。
5. 单边补充后同步应静默合并；两台各改同一个词的近义词后应出现冲突选择，选“两份近义词合并”后两边一致，且修改历史仍在。
6. 导出备份再导入，以及“我的 → 处理本机内容冲突”，都不应影响已补充的近义词。


## 2026-09-17 释义后关联题、题目入口与日期排序（编译修复版 v2.4.2）

- 修复 v2.4.1 在 Xcode 16.4 类型检查中发现的 `extra argument 'errorActivityDate' in call`：`WordRowView` 与 `AdaptiveWordLink` 不再依赖自动成员初始化器，显式支持可选错题日期，并保留所有不传日期的旧调用方式。
- 修复 v2.4.0 在 GitHub Xcode 16.4 归档时发现的三处错误：关联练习 `Section` 泛型无法推断、关联题列表初始化参数标签不匹配，以及错词本大型视图表达式类型检查超时。错词页已拆分为控制区、列表区、行视图和专项刷题动作，交互与数据格式不变。
- “巩固薄弱词”和提醒巩固仅在查看释义后展示关联题卡：先显示不含答案的题干预览，再进入原有作答页；作答前不显示正确答案或解析，提交后仍正常记入答题记录和错词本。
- 词条详情增加“查看关联题目”列表并标记手动题；列表可完整查看题目与解析或开始作答。刷题页显示“查看我添加的题目”入口；题目管理可按最近/最早更新日期排序并显示日期。
- 错词本增加最近/最早错题日期排序，列表显示最近错题日期。结果按排序条件缓存，学习记录或内容变动后才失效；关联题列表在单次渲染中只排序一次。
- `tests/test_reinforcement_and_sorting.py` 防止关联题入口、答案隐藏边界、日期排序和缓存失效回退。
- 本地已通过36项 Python 回归、865词/974题内容与工程资源检查、31个 Swift 文件语法解析及 `git diff --check`。Windows 语法解析不能替代 Xcode 类型检查；`v2.4.2` 的原生归档和 IPA 仍以新 Tag 的 GitHub Actions 结果为准。

## 2026-09-14 新增词条后立即录题（源码计划版本 v2.3.3）

- 词库“添加词条”保存成功后，先定位新词；待词条编辑页关闭后再显示“立即添加题目 / 稍后再说”，避免两个 sheet 同时展示。
- 选择立即添加时，题目编辑器预填新词 UUID 为关联词；取消题目不会删除已经保存的词。使用已有同名词不会出现该提示。
- 手动添加或编辑词条的任意保存、校验错误均以系统弹窗即时说明；同名词提示引导打开已有词条补充笔记或题目，不再要求滚到表单底部查看红字。
- 题目编辑器里“先添加一个新词”仍保留，保存或使用已有词后会自动关联回正在编辑的题目；词条详情仍能为当前词直接添加题目。
- `tests/test_manual_content_flow.py` 检查上述两个方向和词条详情入口，避免连续录入功能回退。

## 2026-09-14 连续学习卡顿与点击反馈（源码计划版本 v2.3.1）

### 问题定位与改动边界

- 用户在 v2.3.0 深度使用并添加个人内容后，连续词卡学习会明显掉帧，页面切换延迟；“忘记 / 模糊 / 记得”缺少可见按压反馈。卸载重装不能修复源码中的重复计算路径。
- 原实现每次学习反馈都执行完整快照比较与多轮校验、重新投影个人目录、构建全部搜索文字、重算完整学习历史，并逐条重建题目关联；随后立即删除和登记整周通知。内置题的关系查询还会为每道题重新构建一次800词名称字典。
- 内容新增/编辑/同步仍先走完整业务验证，但存储层不再重复解码校验同一份有效结果；普通答题、收藏、掌握程度、错误次数、笔记和词卡反馈保存成功后，只增量应用这一条事件，不再重建内容目录和搜索索引。连续保存复用本次运行内已验证的主文件，原子保留上一份有效记录；发现文件被外部改动则自动退回完整校验。事件时间戳最大值、每日统计、题目—词条双向关系和练习筛选关系均缓存复用。
- 连续操作触发的提醒刷新在8秒窗口内合并，避免快速学习时每次点击都重排7条通知。保存仍在切换下一词前完成，历史版本、备份原子性和同步预览失效规则未放松。
- 首页今日词数/答题数和正确率在载入或跨日时汇总，之后随单条事件增量维护；答题历史通过 UUID 索引直接取题，通知跳转和学习进度恢复通过 UUID 索引直接取词。高频写入使用内部可变学习状态，不再因值语义复制完整答题数组和学习字典，每次快照变化也只发送一次通用刷新信号。
- 词卡三个反馈按钮增加触摸缩放、颜色/描边变化、轻触反馈与“正在记录”状态，并阻止重复点击；首页卡片、词条行和答题选项也有即时按压反馈。减少动态效果设置开启时不执行缩放动画。

### 新增原生回归（需在 GitHub macOS 执行）

- LearningEngineTests 使用同一组答题、收藏、词卡和笔记事件，比对逐条增量应用与完整历史重算的错误次数、掌握程度、收藏、笔记、复习时间及答题编号，必须完全一致。
- SnapshotStoreTests 记录验证器调用次数，要求已验证保存不能再次解码完整历史，同时确认上一份主记录仍作为恢复快照保存。
- 同一测试确认缓存后的内置题关联仍指向原内置词，不会被同名个人词截获。现有旧题版本计分、同步、个人内容和存储测试继续由 tools/verify_macos.sh 执行。
- Windows 没有 swiftc / Xcode，以上原生测试尚未在本机编译；Swift 语法解析不能替代类型检查、iOS 编译、帧率和触觉反馈验收。

### 本地已执行

- Python 全量回归25项通过。内容与工程检查通过：865词、974题、原 PDF 文本与哈希、资源布局、工程引用、Scheme、Info.plist 和图标。
- 31个 Swift 文件语法解析通过；`git diff --check` 通过。本轮未修改 Bundle Identifier、Info.plist、备份格式、同步协议或 GitHub 打包工作流。

### 真机待验收

按 [v2.4.2 性能与交互验收清单](docs/performance-device-checklist.md) 在保留原数据的覆盖安装后连续测试。必须以实际 iPhone / iPad 操作确认卡顿改善；本地静态结果不能证明真机帧率。

## 2026-09-13 手动添加词题（源码计划版本 v2.3.0）

### 验证边界

- 本次在用户指定的本地工程直接修改；未提交、创建 Tag、推送或触发远程构建。起始 HEAD 为 fed8390d7ebaac08b42127b854b452da516f8065。
- Windows 没有 swiftc / Xcode，新增原生行为测试按先写测试的顺序加入，但无法在本机执行原生 red/green。所有原生编译、行为测试、iOS archive 和 IPA 安装成功均须另有 GitHub / 真机证据。
- Python 回归和 Swift 语法解析不能证明 Swift 类型检查、UI 操作或双机连接成功。不得把以前版本的绿色构建当作本次的构建结果。

### 核心原生测试覆盖（需在 macOS 执行）

- PersonalLibraryTests：词题输入限制、稳定词条 ID、题目版本、归档恢复、完整目录引用、无环父修订图、同名冲突、v2/v3 编码及数量/字节边界。
- SnapshotStoreTests：真实临时目录的 v2 迁移、v3 主文件/上一份备份、损坏证据保留、禁止静默回退 v2、验证/写入失败；未来版本主文件/备份/旧版迁移入口明确拒绝，并比较拒绝前后的文件清单与字节，确保不被覆盖。仅含未来版本标识、不含当前字段的文件也受保护。
- LearningEngineTests：修改新题的答案或关联词后，旧答题仍按原版本计分并归属原词；保留原有复习规则回归。
- PersonalSyncTests：独立新增、后继与并发修订、归档冲突、同名词选择、重复同步、完整历史、预览过期、最终方案验证、合并/解决冲突后的总量限制。
- 同名选择额外覆盖双方离线作出相反选择：保留活跃词的决定也形成修订，再合并时产生明确冲突，不能静默变成两条都归档。
- Unicode 边界覆盖看起来相同但 UTF-8 不同的词义、笔记和选项；相同编号不能替换原始载荷。记录数组顺序改变仍可接受。
- SyncExchangeTests：协议版本 2 / 内容格式能力 3，旧版或缺少能力信息时拒绝；内置资料指纹不包括个人新增内容。
- PersonalTransactionTests：新词与原格式笔记单次保存、笔记100000字节边界、捕获修订头后拒绝过期编辑、规范化重名、答案必须明确选择、重复保存拒绝、旧题版本计分、归档恢复和新练习过滤。
- 原有安全握手、两端确认、断线和布局规则测试继续由 tools/verify_macos.sh 调用。

### 本地已执行

- 最终存储修正后，控制端重新运行 Python 回归，25项全部通过（8.173秒）。数据与工程检查通过：865词、974题、原 PDF 内容与哈希、资源复制、工程引用、Scheme、Info.plist 和图标。
- 31个 Swift 文件语法解析通过。首次尝试因既有检查依赖的读取权限失败，随后仅提升读取权限重新检查成功；没有安装软件或改动文件权限。此项不是 Swift 类型检查或 iOS 编译。
- 三个实现阶段和整体联动复审已完成。整体审查发现的“未知版本主文件被旧备份替代”问题已修正并通过定点复审，无未解决的 Critical / Important 问题；未知版本错误会阻止恢复和覆盖写入，已知版本损坏恢复保留。真实界面和附近同步按下方清单验收。
- 最终差异格式检查退出0，无空白错误；Markdown 文件有 Git 的 LF/CRLF 转换提示，未更改 Git 配置。build.yml、Info.plist、发布脚本相对起始提交无变化，Bundle Identifier 保持 com.peanut13.words800。
- 一项非阻断优化留待后续：大量内容冲突选项会重复构建名字映射，尚无真机性能测量；设备清单已包含大资料预览响应检查。

### 真机待验收

两台先各自导出备份，再覆盖安装同一份新 IPA。按 [手动词题验收清单](docs/personal-library-device-checklist.md) 检查新增/编辑/归档、题目历史、iPad 旋转草稿、旧备份导入、内容冲突与附近同步。新版备份不保证旧 App 可读，不建议降级。

## 2026-09-13 Release 附件名称与版本（源码计划版本 v2.2.2）

### 本地已执行与改动边界

- 开始时 `main` 工作区干净，HEAD 为 `4e5c5399a9cb7736bf1e95a3769248ac9eef27a7`，已有本地 Tag `v2.2.0` / `v2.2.1`。本次没有提交、创建 Tag、推送、触发远程构建或修改已经发布的附件。
- 新版本页面显示 `政名政利公考800词-vX.Y.Z.ipa`，实际下载文件使用 `ZhengMingZhengLiGongKao800-vX.Y.Z.ipa`；SHA-256 文件、内容中的文件名、Release 链接、构建元数据与作业输出一致。手动分支包额外标明 `manual-提交短哈希`，仍不创建 Release。
- 保留原输出 `build/800词学习助手.ipa`、归档与 IPA 校验、签名选项、Bundle ID、内部产品名及全部 App 源码。本次发布副本只更名，不改 IPA 字节，也不修改学习记录或备份格式。
- 命名测试先在旧代码下失败，确认缺少新文件名/label及链接仍使用旧文件名；实现后 Python3.9 全量25项回归通过，包含两个 Tag 的名称派生、中文显示名/下载地址/GITHUB_OUTPUT联动、手动构建、原字节复制和校验文件等。测试中的 Tag/提交仅在临时测试仓库生成。
- 数据与工程校验通过：865词、974题、PDF 来源与哈希、资源复制、工程引用、Scheme、Info.plist 与图标。
- 使用本地已有 PyYAML 解析实际工作流，检查 Tag/手动触发、只读构建与写权限发布、草稿上传及输出接线；从 YAML 提取实际 JavaScript，用 Node 执行11个离线 API 替身场景：成功、缺附件、重复附件、上传未完成、文件名被改写、畸形 JSON、无效 Release ID、标签 API 失败、返回错误文件名、返回错误标签、最终发布 API 失败。确认只操作 IPA/校验附件，两个标签成功核对后才发送发布请求，失败不输出成功地址。这不是 GitHub 实际 API 联调。
- 独立只读复审未发现 Critical / Important 问题，并独立复跑25项回归与差异格式检查通过。按复审意见补充“未发布草稿可以失败重跑，成功发布后使用新 Tag”的边界。

### 下一次发布待验收

1. 用 GitHub Desktop 提交本次改动，在 History 对该提交创建新 Tag `v2.2.2` 并 Push origin；不要复用旧 Tag。检查 Build IPA 完整运行通过。
2. 发布作业先上传草稿附件，设置并核对两个中文显示名，之后正式发布；若首次发布停在设置名称步骤，解决问题后重跑，不要提前手动公开草稿。
3. `Release v2.2.2` 的 Assets 和正文显示 `政名政利公考800词-v2.2.2.ipa` 及同名校验附件；正文链接、Assets 链接均可下载，实际文件名为 `ZhengMingZhengLiGongKao800-v2.2.2.ipa`，SHA-256 与下载字节匹配。旧版本附件保持原样。
4. Windows 本机没有执行 iOS 原生编译、GitHub 实际发布或真机安装。两台更新前导出备份，再用 TrollStore 覆盖安装；命名变化不改变应用身份。

## 2026-09-13 个人笔记区域上移（源码计划版本 v2.2.1）

### 回归场景与改动边界

- 用户截图中的“一成不变”词条，笔记入口、正文和历史原来位于“我的学习”内部，排在释义、辨析之后；这不符合本次确认的“释义 → 个人笔记 → 解析/辨析 → 我的学习”顺序。
- 本次只调整词条详情的声明式布局，不增加笔记字段，不改记录编号、保存、备份、合并、编辑器保存/取消或 iPad 编辑窗口的持有方式。笔记在独立卡片内直接显示全文，空状态提供添加入口，已有记录提供编辑入口，历史继续折叠展示。
- 布局位置需通过下方真机清单验证，不用源码字符串匹配代替 UI 测试。现有原生 LearningEngineTests / SyncMergeEngineTests 负责笔记修订历史、备份往返与同步冲突等行为回归；Windows 没有 Swift/iOS 运行环境，不能执行这些原生测试。

### 本地已执行

- 修改前工作区干净，本地已有 `v2.2.0` Tag；本次未提交、创建 Tag、推送或运行远程构建。
- 改动前后分别运行 Python3.9 全量回归测试，23项均通过。数据与工程校验通过，22个 Swift 文件语法解析通过；`git diff --check` 通过。
- 这23项 Python 测试不是 UI 渲染测试，不能证明笔记区域位置或编辑体验。此次没有新增数据处理行为，界面顺序、空笔记/长笔记及 iPad 旋转表现按下方真机清单验收；原生学习与同步测试仍由 GitHub 执行。
- 独立只读复审未发现重要问题；笔记绑定、编辑入口、历史记录和 iPad 编辑窗口的持有方式保持不变。

### 真机待验收

1. 覆盖安装前导出备份。词库打开“一成不变”：个人笔记紧跟释义，位于重点解析/关联词与辨析之前；“我的学习”中不再有重复的笔记入口或正文。
2. 无笔记词条显示“添加笔记”；输入多行中文、空行和长段落后保存，卡片显示完整正文、保留换行，可长按选择复制，不截断。
3. 点“编辑笔记”修改后取消，原内容不变；保存后立即显示新内容。清空后回到添加入口，已有笔记历史仍保留；展开历史可查看旧内容和清空记录。
4. 切换不同词条，不显示上一个词的笔记；退出 App 后重开，笔记仍在。此前备份及附近同步仍使用同一份学习记录。
5. iPhone 深浅色/大字号，iPad 横屏双栏、竖屏及窄分屏检查布局。编辑未保存时旋转，草稿保留，取消不写入；从关联词和答题解析打开词条，也显示同样的笔记区域。
6. 刷题作答前不增加笔记或其他答案提示，提交后的解析、错误次数和收藏功能不变。

## 2026-09-12 Tag 发布与应用更名（源码计划版本 v2.2.0）

### 本地已执行

- 修改前工作区干净，当前分支 `main` 跟踪 `origin/main`，HEAD 为 `c5e574b`，本地无 Tag。原工作流仅有分支推送/手动构建，没有 Release，也没有实际使用 `run_number`；本次将正式发布改为 Tag 驱动。没有查询远程 Tag、修改远程地址、提交、打 Tag、推送或创建远程 Release。
- 保留原 unsigned archive、ARM64、iPhone/iPad 共用 Scheme 和扁平资源布局，原输出仍为 `build/800词学习助手.ipa`；发布副本为 `build/release/Words800App.ipa`。Bundle Identifier 仍为 `com.peanut13.words800`，学习数据格式和存储标识未改动。
- `python tools/validate_project.py` 通过：865词、974题、原 PDF 来源、工程引用、共享 Scheme、Info.plist、图标和资源复制布局。
- Python3.9.13 与3.12均运行 `python -B -m unittest discover -s tests -p 'test_*.py' -v`，23项全部通过。其中原回归7项，发布/更名13项，安装包版本边界3项。
- 发布测试覆盖轻量/附注 Tag、版本不随运行次数变化、手动分支不发布、手动 Tag 发布、非法 Tag、Tag 不存在或不指向当前提交、重复版本记录拒绝、README 代码块排除、真实 README/模板联动、提交历史回退、制品原字节复制、SHA-256 和缺包拒绝。所有测试 Tag/提交仅在测试创建的临时 Git 仓库中生成，不操作用户仓库。
- 归档版本边界测试在新增检查前3项失败，实现后通过。首次发布 CLI 测试在未实现时失败；审查又复现 Python3.9 下8项失败，原因是 `Path.write_text(newline=...)` 不兼容，改为文件 `open` 后全量通过。
- tree-sitter 解析22个 Swift 文件通过。版本读取的类型转换显式加括号，消除本地解析器歧义。此检查不等于 Swift 类型检查、链接或 iOS 编译。
- 用仅安装在 `tmp/release-validation-deps` 的 PyYAML6.0.2 解析实际工作流，通过 Tag/手动触发、build 只读权限、release 写权限与成功构建依赖的断言。此项为本地静态检查，未在项目运行或 CI 中增加 Python 第三方依赖；不是 GitHub 实际执行验收。
- `git diff --check` 通过。Git 的 LF/CRLF 提示不是检查错误，代码换行规则继续由现有 `.gitattributes` 管理。
- 独立只读复审完成，再次运行 Python3.9 全量23项通过；文档、模板、工作流和更名接线一致，无未解决的 Critical / Important 问题。

### GitHub 与真机待验收

1. 将全部改动提交到 `main`，按 README 推送新的 `v2.2.0` Tag。确认普通分支推送不自动构建，Tag 推送会启动 Build IPA。
2. GitHub 运行全部 Python 和原生 Swift 测试、Xcode16.4 archive、原生 Bundle 检查、IPA 校验。包内版本与构建版本均应为2.2.0，名称为“政名政利公考800词”，Bundle ID 不变。
3. 构建失败时不得进入发布作业；成功后创建 `Release v2.2.0`，页面含该版本 README 更新内容，下载链接、IPA、校验文件和构建信息完整。下载后的 SHA-256 应与附件值一致。
4. 分支手动构建只生成 Actions Artifacts、不产生 Release；非法版本 Tag 在 prepare 阶段拒绝，避免发布错版安装包。代码改动后使用新 Tag，不强制移动已发布 Tag。
5. 两台先导出备份，再通过 TrollStore 覆盖安装同一 IPA。检查桌面名称、首页“政名政利公考”、版本页2.2.0；原笔记/收藏/错误次数仍在，并按下方清单验收 iPad 横屏和附近同步。

Windows 本机未执行 iOS 原生编译、实际 Release 发布或真机覆盖安装；以上测试通过不能替代这三项验收。

## 2026-09-12 版本2.1：iPad 横屏与附近同步

- 用户确认 iPad Pro 2021 / iPhone 14 Pro Max，系统16.5；明确不加二维码，使用 Apple 官方附近发现。
- 本地全量数据和工程检查通过：865词、974题、原 PDF 每条来源一致，16个 App Swift 源文件均注册 Sources，资源仍分别复制到包根目录。
- Python 回归7项通过（资源布局2项、设备能力2项、无题干提示3项）。新增局域网权限测试在旧 plist 下失败，补充声明后通过。
- tree-sitter 解析22个 Swift 文件通过。Windows 无 Swift / Xcode，此项只证明语法可解析，不证明类型、链接或运行时正确。
- 新原生测试先写入再实现，因没有本机 Swift 编译器，未执行原生 red/green；GitHub 的 tools/verify_macos.sh 已包含原有学习测试和新增合并、协议、安全、布局测试。
- SyncMergeEngineTests：公共记录去重、交换顺序一致、重复导入、独立答题、并发笔记合并/清空、手动清零、双端同值调整、手动次数与新错题冲突、解决后重试、篡改 ID、过期预览、禁止丢弃历史、v2往返。
- SyncExchangeTests：有界二进制消息、正确的双方确认顺序、截断/畸形/异任务消息、重复确认、取消，题库指纹匹配/缺失/不一致。附近同步在处理数据前校验两台内置题库内容的 SHA-256。
- NearbySecurityTests：真实 CryptoKit 密钥/配对码一致、确认前禁止数据、承诺/签名验证、身份/角色验证、双向认证加密、篡改/重放/乱序/跨会话拒绝、大小边界。
- AdaptiveLayoutTests：两种尺寸 iPad Pro 的横竖屏、iPhone、窄窗口和宽度阈值共9组规则。
- 只读审查及复审完成；修正横竖屏重建详情时丢失未保存笔记/次数的结构风险，编辑器归稳定浏览器持有，草稿仅首次展示初始化。实际旋转过程仍按下述清单验收。

### 本版尚未执行的验收

1. GitHub 对上述 Swift 测试实际编译运行，再执行 Xcode16.4 Release archive、Bundle/IPA 校验。
2. 两台覆盖安装2.1，首次启动原笔记/收藏/错题仍在；两种 iPad 尺寸按实际拥有的设备验收即可。
3. iPad 横屏：词库和错词左列表/右详情，刷题左题干/右选项；两种横屏方向、竖屏、分屏窄窗口、大字号检查无遮挡。练习中旋转后题号、已选答案和已提交状态不重置，答错不重复记录。编辑笔记/次数输入未保存内容后横竖屏往返，草稿应保留；取消不改原值。详情切换单双栏时允许滚动位置/关联词导航层级复位。
4. 两台同 Wi-Fi、路由器断开互联网：授权局域网、单端发起邀请、首次双端核对配对码、记住配对后重连成功。只在一端忘记配对后，两边仍能看见码并重新确认。
5. 两边各答一题，同步后都保留两个不同尝试；再次同步统计不变。收藏、笔记、掌握程度与学词反馈按规则合并。
6. 共同错误次数5：手机手动清零、平板改7；同步时选择0，两端最终均0，错题历史保留。两端同时从5改7不能变成9。共同词两端分别改笔记，选择拼接后两份都保留。
7. 否认配对码、拒绝邀请、拒绝方案时不导入。发送中/确认前/一端保存后分别锁屏断线，按提示重连可收敛；不把只保存一端显示为双方完成。
8. 预览后产生新记录时，拒绝旧预览覆盖。异常备份、版本不匹配、超过100 MB、同 ID 内容篡改都明确报错而非清空记录。
9. 拒绝局域网权限时有可操作提示；回设置允许后重新寻找。两台提醒开关各自保持，默认时间20:00未改变；无相机权限请求、无二维码。

以上双机网络、权限、画面与安装测试尚未执行，不能用 Python 或语法检查代替。

## 2026-09-12 删除作答前答案提示

- 删除865道释义自测题干末尾的首字、字数提示，并修改 tools/build_content.py，重新生成不会恢复提示。
- 题干正常释义、选项、正确答案、题目 ID 和提交后的解析保持不变；不更改学习记录模型。
- 新增 tests/test_question_prompts.py：检查内置题库不含答案提示、截图“凝视”题的 ID/选项/答案保持兼容，以及独立运行生成脚本不会再添加提示。旧题库和旧脚本下3项测试失败，修正后通过。
- 整个题库与修改前的内容比较，除删除865行提示外没有其他数据变化。
- 无 Swift 源码改动；新 IPA 需由用户用 GitHub Desktop 推送后构建，尚未在本地进行 iPhone 安装验证。

## 早期版本 Windows 已执行（历史记录）

- 原 PDF 重新提取：28页、874条来源记录、865个独立词条，重复项保留出现页码。
- 内容检查：词条/题目 ID 唯一且有效、四个互不重复选项、正确答案范围、题目关联词存在、题目来源和解析非空。
- 原 PDF SHA-256 与词库指纹一致，逐页数量与导入报告一致。
- 工程对象引用全部可解析，共享 Scheme 指向正确目标；所有9个 App Swift 源文件加入 Sources，library.json 和 source.pdf 分别加入资源复制阶段。
- Info.plist 解析与版本检查、图标 PNG 尺寸及 RGB 无透明通道检查。
- tree-sitter Swift 语法检查：9个 App 源文件加1个测试文件通过。此项不是 Swift 类型检查或 Xcode 编译。

## 用户提供的 GitHub 日志（2026-09-12）

日志文件：logs_93958996668.zip。本次没有代替用户操作远程仓库。

- 内容检查、plutil 工程检查、共享 Scheme 枚举通过。
- 实际 Swift 学习规则测试显示 PASS: 15 learning-engine assertions。
- iPhone ARM64 Swift 编译、链接和资源处理已完成。
- 归档结束阶段失败：Archive Missing Bundle Identifier，exit code 70；尚未产生可下载 IPA。

原因定位：日志明确复制出 Words800App.app/Resources。Apple 的 [Bundle Structures](https://developer.apple.com/go/?id=bundle-structure) 明确禁止 iOS App 包内自定义 Resources 文件夹。本地 Info.plist 及构建日志均存在 com.peanut13.words800，不是缺少 Bundle ID 配置。

修正：源码 Resources 目录保留为 PBXGroup，按文件复制 library.json / source.pdf 到 App 包根目录；App 使用 Bundle 根资源查找；IPA 校验读取新位置并拒绝旧布局。没有修改 Bundle ID、签名要求、词库内容或用户学习记录。

本地回归：tests/test_bundle_layout.py 的两项检查在旧配置下均失败，修正后均通过。检查实际资源构建阶段的复制目标，不把源码目录误当成最终包布局。

## 修正后的 GitHub 工作流待验证

1. 标准库内容/工程检查、资源布局回归测试、plutil 工程校验及共享 Scheme 枚举。
2. 实际 Swift 编译运行15个学习规则断言：答错累计、答对不累计、答对首次安排复习、手动增减/清零保留历史、非负次数、忘记10分钟、连续记得延长间隔、笔记顺序/历史、收藏取消、备份编码往返、实际答题统计。
3. iPhone Release archive 真机编译。
4. 使用原生 Foundation Bundle 读取归档的 Bundle ID、执行文件、JSON 和 PDF。
5. IPA CRC、Payload 目录、ARM64 Mach-O、Bundle ID、最低 iOS 版本、根目录词库/PDF 字节完整性和编译图标检查。

修正后的 iOS 编译、原生 Bundle 验证、归档和 IPA 安装尚未执行。此前日志通过的测试不等于修正后的完整打包通过。

## iPhone 14 Pro Max / iOS16.5 待验收

- TrollStore 安装，首次打开无闪退；五个模块、深浅色模式、大字体与键盘操作。
- 搜索“源远流长”、释义“历史悠久”、部分词形；分类切换与原资料删除词开关。
- 手动设置错误次数为5、2、0，排序符合预期，清零不删除既有错题；重启后保留。
- 答错一题只记录一次；查看正确答案及解析，错词关联正确。
- 词卡先隐藏答案，反馈后切换下一词；中途退出后继续；到期词与每日计数正确。
- 个人笔记、笔记历史、收藏和掌握程度重启后保持。
- 导出备份；更改记录后导入旧备份不覆盖新事件；相同文件导入两次不重复计数；畸形备份明确拒绝。
- 允许通知并临时设为几分钟后，锁屏核对内容、点击进入对应词卡，再改回20:00；关闭提醒应移除待发通知。
- 禁止通知时提示开启设置；飞行模式下学习、查看 PDF、保存记录可用。
- 覆盖更新安装前备份，确认更新后本地数据保留。
