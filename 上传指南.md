# 用 GitHub Desktop 上传，用 Tag 发布 IPA

操作由你完成。本次没有替你提交、推送或运行远程构建，不需要使用“推送到GitHub.bat”。你不需要安装、打开或操作 Xcode；所有工程编译和 IPA 打包都由 GitHub Actions 自动完成。

## 1. 选择本地工程

在 GitHub Desktop 中打开本地仓库：

`C:\Users\ASUS\Desktop\花生800词\Words800App`

不要选上一级“花生800词”，也不要选里面第二层只放 Swift 文件的“Words800App”。

这次发布流程更新应一起提交，尤其是：

- `.github/workflows/build.yml`
- `.github/release-template.md`、`tools/release.py`
- `README.md` 的更新日志和发布说明
- `Words800App.xcodeproj/xcshareddata/xcschemes/Words800App.xcscheme`
- `Words800App/LearningEngine.swift`
- `Words800App/AdaptiveLayout.swift`、`SyncMergeEngine.swift`、`SyncExchange.swift`
- `Words800App/NearbySecurity.swift`、`NearbyTransport.swift`、`NearbySyncModel.swift`、`NearbySyncView.swift`
- `Words800App/Resources/library.json` 和 `source.pdf`
- `Words800App/Assets.xcassets` 下全部图标文件
- `tools/validate_project.py`、`tools/verify_macos.sh`、`tests/LearningEngineTests.swift`
- `tests/` 下新增的同步、安全与布局测试，以及更新的 `project.pbxproj` / `Info.plist`
- `content/` 中的内容文件与导入报告

本项目的 `.gitignore` 已排除 `tmp/`、构建产物和学习备份。不用上传本地校验依赖或临时 PDF 图片，也不用清空远程仓库。

## 2. 提交与推送

先在 README 的“更新日志”最上方写入本次版本变化，再填写提交说明，例如 `release v2.2.0`，提交后使用 Desktop 的推送按钮。当前内部版本为2.1，本次建议从正式 Tag `v2.2.0` 开始；后续使用新的、更高的版本号。

如 Desktop 显示的远程仓库不是你准备上传的仓库，在 Repository Settings → Remote 中核对地址；本次没有替你修改远程地址。不要把访问令牌写进源码、命令或仓库地址。

## 3. 推送 Tag，自动构建并发布

Desktop 提交并推送完成后，在此仓库目录的终端执行：

```bash
git tag v2.2.0
git push origin v2.2.0
```

只推送 `main` 不会构建；推送上述新 Tag 后，GitHub 才自动构建 IPA 并创建 `Release v2.2.0`。不要提前打 Tag，Tag 指向的提交必须包含本次所有改动。不要覆盖已存在的 Tag。

如果不用 Desktop 提交，也可以全程使用以下命令（两种提交方式选一种即可）：

```bash
git add .
git commit -m "release v2.2.0"
git tag v2.2.0
git push origin main
git push origin v2.2.0
```

到仓库网页 → Actions → Build IPA 查看进度。若只想测试，可选分支点击 Run workflow；分支手动构建不创建 Release。若找不到工作流，先确认仓库根目录有 `.github/workflows/build.yml`，并确认默认分支已包含此文件、仓库允许 Actions。发布所需的 `contents: write` 已在工作流中声明；若组织策略禁止该权限，需在设置中放行。

工作流使用 GitHub 的 macOS 环境和 Xcode16.4，不使用你的 Windows 电脑编译，也不需要添加 Apple 账号、开发者证书或签名 Secrets。

## 4. 下载并安装

正式 Tag 构建和发布均成功后，打开仓库网页 → **Releases → Release v2.2.0 → Assets**，直接下载 **Words800App.ipa**。不用下载 Source code ZIP。Release 页面也有更新说明、SHA-256 校验文件和构建信息。

只有手动分支测试或需要备用制品时，才到 Actions 对应运行的 Artifacts 下载 `Words800App-<Tag或manual提交标识>-IPA`。解压外层 ZIP 后包含：

- `Words800App.ipa`
- `Words800App.ipa.sha256`（校验值，可保留）
- `release-notes.md` 和 `release-metadata.json`

不要把外层 ZIP 改名成 IPA。将真正的 IPA 保存到 iPhone / iPad“文件”中，使用 TrollStore 导入安装。两台安装同一份新版 IPA；手机/平板不需要分别打包。桌面名称是“政名政利公考800词”，首页标题是“政名政利公考”。私有仓库的 Release 需要先登录有访问权限的 GitHub 账号才能下载。

首次打开后，在“我的 → 定时巩固”开启提醒并允许通知，默认每天20:00。学习、记录保存和提醒都不需要联网；查看真题公开网页是可选的外部链接。

iPad 横屏会自动切换适合宽屏的布局。两台数据互传在“我的 → 附近设备同步”，详见 [附近同步使用说明](附近同步使用说明.md)。不需要扫码、相机、Apple账号或云服务。

## 如果失败

点击红色失败步骤，把该步骤第一条明确的 error 及后面相关几行发来。若已经进入编译阶段，也可下载 build-diagnostics 里的 xcodebuild.log。不要只截图最后的“exit code 65”，它通常不含真正原因。

网络或权限故障解决后可以重跑原任务；如果修改了代码，仅在旧失败记录上点 Re-run jobs 会继续使用旧代码，需要重新提交并推送一个新 Tag，例如 `v2.2.1`。不要强制改动已发布版本的 Tag。

2026-09-12 旧日志中的 `Archive Missing Bundle Identifier` 曾定位到包内资源目录布局。本次保留已修正的布局，不修改 Bundle ID。发布流程的本地测试不能代替 GitHub 实际归档和真机安装，请以新 Tag 的绿色构建及设备运行结果为准。

更新安装前建议先在“我的”导出学习备份；不要为了升级先卸载应用，卸载可能删除本地学习记录。
