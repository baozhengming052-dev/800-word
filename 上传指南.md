# 用 GitHub Desktop 上传，再打包 IPA

操作由你完成。本次没有替你提交、推送或运行远程构建，不需要使用“推送到GitHub.bat”。你不需要安装、打开或操作 Xcode；所有工程编译和 IPA 打包都由 GitHub Actions 自动完成。

## 1. 选择本地工程

在 GitHub Desktop 中打开本地仓库：

`C:\Users\ASUS\Desktop\花生800词\Words800App`

不要选上一级“花生800词”，也不要选里面第二层只放 Swift 文件的“Words800App”。

检查 Changes 列表包含新文件，尤其是：

- `.github/workflows/build.yml`
- `Words800App.xcodeproj/xcshareddata/xcschemes/Words800App.xcscheme`
- `Words800App/LearningEngine.swift`
- `Words800App/AdaptiveLayout.swift`、`SyncMergeEngine.swift`、`SyncExchange.swift`
- `Words800App/NearbySecurity.swift`、`NearbyTransport.swift`、`NearbySyncModel.swift`、`NearbySyncView.swift`
- `Words800App/Resources/library.json` 和 `source.pdf`
- `Words800App/Assets.xcassets` 下全部图标文件
- `tools/validate_project.py`、`tools/verify_macos.sh`、`tests/LearningEngineTests.swift`
- `tests/` 下新增的同步、安全与布局测试，以及更新的 `project.pbxproj` / `Info.plist`
- `content/` 中的内容文件与导入报告

本项目的 `.gitignore` 已排除 `tmp/`、构建产物和学习备份。不用上传本地校验依赖或临时 PDF 图片，也不用清空远程仓库。

## 2. 提交与推送

填写一次提交说明，例如“完善离线学习和 IPA 打包”，提交后使用 Desktop 的推送按钮。

如 Desktop 显示的远程仓库不是你准备上传的仓库，在 Repository Settings → Remote 中核对地址；本次没有替你修改远程地址。不要把访问令牌写进源码、命令或仓库地址。

## 3. 在 GitHub 运行构建

打开你自己的仓库网页 → Actions → Build IPA。

推送到 main / master 会触发构建；也可使用 Run workflow 手动运行。若找不到工作流，先确认仓库根目录有 `.github/workflows/build.yml`，并确认默认分支已包含此文件、仓库允许 Actions。

工作流使用 GitHub 的 macOS 环境和 Xcode16.4，不使用你的 Windows 电脑编译，也不需要添加 Apple 账号、开发者证书或签名 Secrets。

## 4. 下载并安装

构建显示绿色成功后，进入该次运行的 Artifacts，下载 **800词学习助手-IPA**。

解压下载的外层 ZIP，里面是：

- `800词学习助手.ipa`
- `800词学习助手.ipa.sha256`（校验值，可保留）

不要把外层 ZIP 改名成 IPA。将真正的 IPA 保存到 iPhone / iPad“文件”中，使用 TrollStore 导入安装。两台安装同一份2.1版 IPA；手机/平板不需要分别打包。也可以直接在设备浏览器下载并解压外层 ZIP。

首次打开后，在“我的 → 定时巩固”开启提醒并允许通知，默认每天20:00。学习、记录保存和提醒都不需要联网；查看真题公开网页是可选的外部链接。

iPad 横屏会自动切换适合宽屏的布局。两台数据互传在“我的 → 附近设备同步”，详见 [附近同步使用说明](附近同步使用说明.md)。不需要扫码、相机、Apple账号或云服务。

## 如果失败

点击红色失败步骤，把该步骤第一条明确的 error 及后面相关几行发来。若已经进入编译阶段，也可下载 build-diagnostics 里的 xcodebuild.log。不要只截图最后的“exit code 65”，它通常不含真正原因。

2026-09-12 日志中的 `Archive Missing Bundle Identifier` 已定位到资源目录布局。此次本地改动包含工程文件、资源读取代码、工作流和校验脚本，请一起提交后推送；仅在旧失败记录上点 Re-run jobs 会继续使用旧代码。推送后打开对应新提交的构建记录。

用户提供的原构建日志显示 Swift 编译与15项学习规则测试通过，但修正后的完整归档、IPA 生成和真机安装仍需验证。请以新提交的绿色构建和手机实际运行结果为准。

更新安装前建议先在“我的”导出学习备份；不要为了升级先卸载应用，卸载可能删除本地学习记录。
