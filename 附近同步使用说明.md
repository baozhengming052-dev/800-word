# iPhone 与 iPad 附近同步

适用：iPhone 14 Pro Max / iPad Pro 2021，系统16.5，TrollStore 安装同一份新版 IPA。同步不需要账号、服务器或 iCloud；不包含电脑端同步，也不使用扫码。

## 第一次连接

1. 更新前，两台分别在“我的”导出学习备份。使用 TrollStore 覆盖安装新版，不要先卸载旧 App。
2. 两台开启 Wi-Fi、蓝牙，放在旁边；优先连接同一 Wi-Fi，路由器可以没有互联网。
3. 两台都进入“我的 → 附近设备同步”，点“开始寻找附近设备”。首次系统询问“本地网络”时选允许。
4. 在其中一台点另一台设备名称；另一台接受邀请。不要在两台同时发邀请。
5. 首次配对，两边显示六位配对码。确认完全一致后，两边都点确认；不一致就取消。设备名称不能代替配对码。
6. 发起方看到合并概况。若自建词或题目有独立修改，先核对内容、归档状态及同名词的选择；再处理笔记、补充近义词和手动错误次数冲突。笔记和近义词都能拼接保留两份。
7. 发起方发送合并方案，另一台查看变化并点“确认变化并保存到本机”。保持两边在这个页面，直到完成提示。

正常情况下，下次连接只需接受邀请，不再手动核对已配对身份。若一台忘记了配对或身份改变，会重新要求核对；另一台仍显示配对码供比较。

## 什么会合并

- 答题记录、错题历史、错误次数、收藏、掌握程度、个人笔记与笔记历史、补充的近义词与近义词修改历史、学词反馈和复习记录。
- 手动添加的词语、题目及其全部内容修订、归档状态。没有答题记录的新词题也会同步；预览分别列出词题变化和学习操作数量。
- 相同记录按编号只保留一次；两台分别答的题都保留。重复同步、重复导入不会重复累加。
- 手动次数有冲突时选择的是最终总数，不是增量。选择0会保留错题历史。
- 两边各改过同一个词的笔记且内容不同，会要求选择；旧版本仍在词条的笔记历史中。近义词同理，选择“两份近义词合并”时同名条目保留本机写法，只补充对方独有的，旧内容仍留在修改历史中。
- 收藏、掌握程度和复习安排沿用事件时间顺序重放。建议两台均启用系统自动日期与时间。

提醒时间/开关、每日目标、筛选条件、尚未完成的题组/词卡队列属于设备设置，不同步。想避免两台同时提醒，可以只开启其中一台的提醒。内置词题和 PDF 由同版 IPA 自带，不在每次同步中传输 PDF；你手动添加的词题则随记录一起传输。

## 自建内容与升级

带手动词题功能的 v2.3.0 源码使用新版学习备份（schema 3）和同步协议；两台都需更新到包含该功能的同一版本，再开始附近同步。个人词题不同是正常待合并状态，不要求两台预先录入相同内容。

补充近义词功能（v2.4.3）把同步的内容能力从 3 升到 4：两台都装 v2.4.3 才能同步。只升级一台时，未升级的一端会在开始交换前提示“同步协议或个人内容格式不一致，请更新两台设备的 App”，不会收到无法解析的记录，也不会写入任何内容。

- 一方沿着旧内容继续编辑时，使用后继修订；双方独立编辑时，查看各版本并选择，不按设备时间静默覆盖。未选版本仍留在备份中。
- 两台独立建立同名词时，选择一条作为活跃词，另一条归档并保留原历史，不把两条的错误次数相加。内置词不会被自建词覆盖。
- 修改题目答案或关联词后，过去成绩仍按作答时的题目版本计算。归档不会删除旧答案或解析。
- 新版可以导入旧 v2 备份并合并学习记录，不清空本机自建内容。新版导出的 v3 备份不保证旧 App 可读，不建议降级。
- 本机使用独立的 study-v3.json，保留旧 v2 文件；更新前仍应手动导出备份。普通 JSON 备份包含私人词题和笔记，请妥善保存。

上述新增流程需用构建成功的新 IPA 真机验收，清单见 [手动词题验收](docs/personal-library-device-checklist.md)。

## 断线与失败

退出同步页面、锁屏、切到后台会停止连接。没有后台自动同步，也不能在一台不在身边时远程同步。

确认保存前，原记录不变。两台分别保存，不存在跨设备的“一起写入磁盘”：如果一台已经保存、另一台还没确认就断线，会明确显示未完成；重新连接同步即可补齐，已有记录按 ID 去重。不要用卸载重装解决同步问题。

同步期间如果本机出现新学习记录或词题修订，旧预览不能覆盖新内容，App 会停止保存并要求重新同步。v2.4.8 起备份限制 100 MB、单张笔记图片限制 5 MB，学习事件加个人内容修订合计最多 10 万条；两台设备都需更新到同一版本才能附近同步。格式损坏、同编号不同内容、内置词库不匹配或版本不支持都会拒绝。

找不到设备时：两台重新点寻找；到系统设置中检查本地网络权限；使用同一个非访客 Wi-Fi，避免路由器的设备隔离设置。Wi-Fi/蓝牙负责附近通信，并不代表要访问互联网。无法使用附近连接时，仍可用“导出学习记录 / 导入学习备份”做人工文件合并。

“忘记已配对设备”只清除本机的信任记录，不删除学习数据；另一台也可单独忘记配对。学习备份是包含私人笔记的普通 JSON 文件，请自行妥善保存，不要上传公开仓库。

## 开发与验收说明

底层使用 Apple [Multipeer Connectivity](https://developer.apple.com/documentation/multipeerconnectivity)，声明其要求的[本地网络与 Bonjour 权限](https://developer.apple.com/documentation/technotes/tn3179-understanding-local-network-privacy)。链路要求加密，应用层额外使用 CryptoKit 的签名临时密钥、配对码和带序号认证加密；配对身份保存在本机 Keychain。相机、互联网服务器、远程推送都不参与同步。

Windows 尚不能验证实际 iOS 编译、局域网授权或两台真机互传。请以 GitHub 新提交的绿色构建及两台16.5设备验收为准；检查清单见 TESTING.md。
